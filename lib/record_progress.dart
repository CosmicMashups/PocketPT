// Import packages
import 'package:flutter/material.dart';

class RecordProgressPage extends StatelessWidget {
  const RecordProgressPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}